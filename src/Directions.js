import React from 'react';

export const Directions = (props) => {
    return (
        <div className='directions'>
            <p>
                Put each painting in your own words. Each input may be used for a single word or a short phrase. Have fun!
            </p>
        </div>
    )
}